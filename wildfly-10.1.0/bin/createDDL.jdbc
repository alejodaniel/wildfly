CREATE TABLE userbox (IDUSER INTEGER NOT NULL, EMAIL varchar(100), IDENTIFICATION varchar(15), NAMES varchar(100), PASSWORD varchar(100), ROL varchar(100), USERNAME varchar(100), PRIMARY KEY (IDUSER))
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL(38), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL(38), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
